[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "ai-contained-base"
version = "0.1.0"
description = "Base Docker image with MCP server and provider finalization for AI-Contained"
requires-python = ">=3.12"
dependencies = [
    "ai-contained-core-mcp @ ../core-mcp",
    "fastmcp==3.2.4",
    "starlette",
]

[project.scripts]
ai-contained-server = "ai_contained.base.server:main"
ai-contained-finalize = "ai_contained.base.finalize:main"

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=1.0",
    "pytest-describe>=3.1",
    "assertpy>=1.1",
    "ruff>=0.15.12",
    "mypy>=1.20.2",
]

[tool.ruff]
line-length = 120

[tool.ruff.lint]
select = ["E", "F", "I", "D"]
ignore = ["D203", "D213"]  # conflict with D211/D212

[tool.ruff.lint.per-file-ignores]
"tests/**" = ["D"]  # no docstrings required in tests

[tool.ruff.format]
quote-style = "double"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
markers = [
    "focus: run only focused tests (use with -m focus)",
]

[tool.mypy]
mypy_path = "src"
explicit_package_bases = true
strict = true

[tool.hatch.build.targets.wheel]
packages = ["src/ai_contained"]
